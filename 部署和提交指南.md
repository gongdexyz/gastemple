# Gas Temple - 快速部署和提交指南

## 🚨 当前问题

APK 构建遇到 Java/Gradle 环境问题。推荐方案：**先部署网站，再构建 APK**

## ✅ 推荐流程

### 步骤 1: 部署网站到 Vercel (5 分钟)

**方法 A: 使用 Vercel CLI (最快)**

```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 登录 Vercel (会打开浏览器)
vercel login

# 3. 部署到生产环境
vercel --prod

# 4. 按提示操作:
#    - Set up and deploy? Yes
#    - Which scope? 选择你的账号
#    - Link to existing project? No
#    - Project name? gas-temple (或其他名称)
#    - Directory? ./ (当前目录)
#    - Build command? npm run build
#    - Output directory? dist
```

**方法 B: 使用 Vercel 网页界面**

1. 访问 https://vercel.com/
2. 使用 GitHub 账号登录
3. 点击 "Add New Project"
4. 导入你的 Git 仓库或上传 `dist/` 目录
5. 配置:
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. 点击 Deploy

**部署后获取 URL:**
- Vercel 会提供一个 URL，如: `https://gas-temple.vercel.app`
- 或者配置自定义域名: `https://gongde.xyz`

### 步骤 2: 配置自定义域名 (可选)

如果你有 `gongde.xyz` 域名：

1. 在 Vercel 项目设置中点击 "Domains"
2. 添加域名: `gongde.xyz`
3. 按照提示在域名注册商处添加 DNS 记录:
   - Type: `A`
   - Name: `@`
   - Value: Vercel 提供的 IP 地址
   
   或使用 CNAME:
   - Type: `CNAME`
   - Name: `@` 或 `www`
   - Value: `cname.vercel-dns.com`

### 步骤 3: 更新 TWA Manifest

部署成功后，更新 `twa-manifest.json` 中的 URL：

```json
{
  "host": "你的域名或vercel地址",
  "iconUrl": "https://你的域名/temple.svg",
  "webManifestUrl": "https://你的域名/manifest.json",
  "fullScopeUrl": "https://你的域名/"
}
```

### 步骤 4: 构建 APK

```bash
npx @bubblewrap/cli build --skipPwaValidation
```

**交互式输入:**
- 是否重新生成项目? `n`
- versionName: `1.0.0`
- versionCode: `1`
- Key Store Password: `android`
- Key Password: `android`

## 🔧 如果 APK 构建仍然失败

### 方案 A: 使用 Bubblewrap Web Tool

1. 访问: https://www.pwabuilder.com/
2. 输入你的网站 URL
3. 点击 "Build My PWA"
4. 选择 Android 平台
5. 下载生成的 APK

### 方案 B: 使用 Android Studio

1. 下载安装 Android Studio
2. 使用 Bubblewrap 生成项目: `npx @bubblewrap/cli init`
3. 在 Android Studio 中打开 `android-build` 目录
4. 点击 Build > Build Bundle(s) / APK(s) > Build APK(s)

### 方案 C: 使用 GitHub Actions 自动构建

创建 `.github/workflows/build-apk.yml`:

```yaml
name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
    
    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build web app
      run: npm run build
    
    - name: Build APK
      run: |
        echo "n" | npx @bubblewrap/cli build --skipPwaValidation
    
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: gas-temple-apk
        path: app-release-signed.apk
```

## 📋 提交到 Solana dApp Store

### 必需材料清单

- [x] **APK 文件** - 待构建
- [x] **5 张截图** - `screenshots/` 目录已准备
- [ ] **Publisher Avatar** - 需要创建 512x512 PNG
- [x] **隐私政策** - 部署后可访问
- [x] **EULA** - 部署后可访问
- [ ] **网站 URL** - 部署后获得
- [ ] **联系邮箱** - 需要设置

### 提交信息

```yaml
应用名称: Gas Temple - 功德无量
包名: com.gastemple.app
版本: 1.0.0
分类: Entertainment
年龄分级: 12+

发布者名称: Gas Temple Studio
网站: https://你的域名
邮箱: contact@你的域名 或 Gmail

隐私政策: https://你的域名/privacy-policy.html
EULA: https://你的域名/eula.html
```

### 提交流程

1. 访问: https://dapp-publishing.solanamobile.com/
2. 使用 Solana 钱包登录
3. 填写应用信息
4. 上传 APK 和截图
5. 完成身份验证
6. 提交审核

## 🎯 立即行动

**最快路径 (推荐):**

```bash
# 1. 部署网站
npm i -g vercel
vercel login
vercel --prod

# 2. 记录获得的 URL

# 3. 更新 twa-manifest.json 中的所有 URL

# 4. 构建 APK
npx @bubblewrap/cli build --skipPwaValidation

# 5. 准备 Publisher Avatar (512x512 PNG)

# 6. 提交到 Solana dApp Store
```

**预计总时间:** 30-60 分钟

## 📞 需要帮助?

- Vercel 文档: https://vercel.com/docs
- Bubblewrap 文档: https://github.com/GoogleChromeLabs/bubblewrap
- PWABuilder: https://www.pwabuilder.com/
- Solana dApp Store: https://dapp-publishing.solanamobile.com/

---

**下一步:** 运行 `vercel --prod` 部署网站
