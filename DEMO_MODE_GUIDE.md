# 演示模式使用指南

## 概述

Gas Temple 提供完整的演示模式，让你可以在不消耗真实代币的情况下展示所有功能。

## 演示模式 URL

### 🎯 完整演示模式（推荐）
```
https://你的网站.com?test=demo
```
**启用功能：**
- ✅ 无限功德币（$GONGDE）
- ✅ 免费自动代敲（SKR）
- ✅ 所有功能完全可用

### 🪙 功德币测试模式
```
https://你的网站.com?test=gongde
```
**启用功能：**
- ✅ 功德币余额：999999（无限）
- ✅ 抽签不消耗 GD
- ✅ 木鱼不消耗 GD

### 🤖 SKR 代币测试模式
```
https://你的网站.com?test=skr
```
**启用功能：**
- ✅ 无需连接钱包
- ✅ 无需真实 SKR 代币
- ✅ 所有档位免费使用
- ✅ 自动挂机功能完全可用

## 功能说明

### 1. 功德币（$GONGDE）系统

**正常模式：**
- 初始余额：888 GD
- 抽签消耗：100 GD/次（首次免费）
- 木鱼功德模式：100 GD/次

**演示模式：**
- 余额：999999 GD（无限）
- 所有操作免费

### 2. 自动代敲（SKR）系统

**三个档位：**
1. **33 SKR** - 自动代敲
   - 1倍速
   - 小沙弥为你代劳
   - 持续3小时

2. **58 SKR** - 功德加持
   - 3倍速
   - 功德×3，效率提升
   - 持续3小时

3. **108 SKR** - 方丈加持
   - 5倍速
   - 法力无边，功德×5
   - 持续3小时

**演示模式：**
- 无需连接钱包
- 无需支付 SKR
- 直接启用自动挂机
- 所有档位免费体验

## 演示流程建议

### 方案 A：完整演示（推荐）
1. 访问 `?test=demo`
2. 展示抽签功能（无限 GD）
3. 展示木鱼功能（无限 GD）
4. 展示自动代敲（免费 SKR）

### 方案 B：分功能演示
1. **功德币系统演示**：`?test=gongde`
   - 展示抽签
   - 展示木鱼冥想/功德模式
   
2. **SKR 系统演示**：`?test=skr`
   - 展示自动代敲选项
   - 展示不同档位效果

## 生产环境配置

### 需要配置的地方：

**1. SKR 代币地址**
```typescript
// src/components/WoodenFish.tsx
const RECIPIENT_ADDRESS = '你的Solana钱包地址'
const SKR_TOKEN_ADDRESS = 'SKR代币的合约地址'
```

**2. 网络选择**
- 当前：Solana Mainnet (`https://api.mainnet-beta.solana.com`)
- 测试：Solana Devnet (`https://api.devnet.solana.com`)

## 注意事项

1. **演示模式标识**
   - 启用演示模式后，界面会显示 "🧪 演示模式" 标识
   - 用户可以清楚知道当前处于测试状态

2. **数据持久化**
   - 演示模式下的数据仍会保存到 localStorage
   - 刷新页面后数据保留

3. **真实支付**
   - 移除 URL 参数后，系统恢复正常模式
   - 需要真实代币才能使用功能

## 技术实现

### 测试模式检测
```typescript
// 功德币测试模式
const isTestMode = () => {
  const params = new URLSearchParams(window.location.search)
  const testParam = params.get('test')
  return testParam === 'gongde' || testParam === 'demo'
}

// SKR 测试模式
const isSKRTestMode = () => {
  const params = new URLSearchParams(window.location.search)
  const testParam = params.get('test')
  return testParam === 'skr' || testParam === 'demo'
}
```

### 自动挂机逻辑
```typescript
// 每秒自动点击，根据倍率执行多次
setInterval(() => {
  const multiplier = autoClickMultiplier || 1
  for (let i = 0; i < multiplier; i++) {
    addMerit() // 自动敲木鱼
  }
}, 1000)
```

## 常见问题

**Q: 演示模式会影响真实数据吗？**
A: 不会。演示模式只是跳过支付验证，不会影响区块链或真实代币。

**Q: 如何退出演示模式？**
A: 移除 URL 中的 `?test=xxx` 参数，刷新页面即可。

**Q: 演示模式下可以连接钱包吗？**
A: 可以，但不是必需的。演示模式会跳过钱包验证。

**Q: 自动代敲会一直运行吗？**
A: 不会。设置了3小时有效期，到期后自动停止。

## 更新日志

### 2026-01-25
- ✅ 添加统一演示模式 `?test=demo`
- ✅ 优化 SKR 测试模式
- ✅ 添加演示模式 UI 标识
- ✅ 完善自动代敲功能
